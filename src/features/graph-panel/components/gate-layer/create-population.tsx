export const createPopulation = (props: any) => {
  console.log("createPopulation", props);
};

export const getNewPopulationName = (): string => {
  // return `${xParameterDef.parameterSpec.name}, ${yParameterDef!.parameterSpec.name} subset`;
  return "PopDef";
};
